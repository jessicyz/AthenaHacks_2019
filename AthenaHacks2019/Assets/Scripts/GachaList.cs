using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GachaList : MonoBehaviour
{

    //maximum number of unique villagers in the gacha. Change to accomodate however many there are
    public const int maxNumVillagers = 8;
	//public static FloorInfo[] arr_floorInfo = new FloorInfo[maxNumFloors];

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
